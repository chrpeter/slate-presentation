{
  "name": "slate-presentation",
  "version": "0.1.0",
  "private": true,
  "dependencies": {
    "gh-pages": "^1.1.0",
    "immutable": "^3.8.2",
    "raw-loader": "^0.5.1",
    "react": "^16.2.0",
    "react-app-rewired": "^1.4.0",
    "react-dom": "^16.2.0",
    "slate": "^0.33.0",
    "slate-collapse-on-escape": "^0.6.0",
    "slate-plain-serializer": "^0.5.6",
    "slate-react": "^0.12.0",
    "slate-soft-break": "^0.6.0",
    "spectacle-scripts": "2.0.0"
  },
  "scripts": {
    "start": "react-app-rewired start --scripts-version spectacle-scripts",
    "build": "react-app-rewired build --scripts-version spectacle-scripts",
    "test": "react-scripts test --env=jsdom",
    "eject": "react-scripts eject",
    "predeploy": "npm run build",
    "deploy": "gh-pages -d build"
  }
}
